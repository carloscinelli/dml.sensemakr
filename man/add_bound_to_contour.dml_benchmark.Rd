% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{add_bound_to_contour.dml_benchmark}
\alias{add_bound_to_contour.dml_benchmark}
\title{Add benchmark bounds to an existing contour plot}
\usage{
add_bound_to_contour.dml_benchmark(
  benchmarks,
  kd = 1,
  ky = kd,
  rho2 = 1,
  which.bound = c("lwr", "upr"),
  level = 0.95,
  combine.method = "median",
  label.bump.x = 0.01,
  label.bump.y = 0.01,
  cex.label.text = 0.7,
  round = 0,
  label.text = TRUE,
  bound_label = NULL,
  ...
)
}
\arguments{
\item{benchmarks}{an object of class \code{\link{dml_benchmark}}.}

\item{kd}{numeric vector of multipliers for the treatment sensitivity parameter. Default is 1.}

\item{ky}{numeric vector of multipliers for the outcome sensitivity parameter. Default is \code{kd}.}

\item{rho2}{degree of adversity. Default is 1.}

\item{which.bound}{which bound was used for the contour plot: \code{"lwr"} or \code{"upr"}.}

\item{level}{confidence level. Default is 0.95.}

\item{combine.method}{method for combining cross-fitting estimates.}

\item{label.bump.x}{bump on the x coordinate of label text. Can be a scalar (same for all points) or a vector of length equal to the total number of benchmark points (n_covariates * n_multipliers), ordered by covariate then multiplier.}

\item{label.bump.y}{bump on the y coordinate of label text. Can be a scalar or vector, same as \code{label.bump.x}.}

\item{cex.label.text}{magnification for label text.}

\item{round}{number of digits to show in bound values.}

\item{label.text}{should label texts be plotted? Default is \code{TRUE}.}

\item{bound_label}{optional character vector of custom labels. If \code{NULL} (default), labels are generated automatically. Can be a vector of length equal to the total number of benchmark points.}

\item{...}{additional arguments passed to \code{sensemakr::add_bound_to_contour}.}

\item{model}{an object of class \code{\link{dml}} used to compute bound values.}
}
\description{
This method allows you to add benchmark bounds from a \code{\link{dml_benchmark}} object to an existing contour plot.
It computes bound values automatically using the provided \code{model}. Multipliers \code{kd} and \code{ky} are applied
to all benchmark covariates (cross product), matching sensemakr OLS behavior.
}
