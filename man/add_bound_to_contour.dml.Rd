% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{add_bound_to_contour.dml}
\alias{add_bound_to_contour.dml}
\title{Add bound to an existing contour plot}
\usage{
add_bound_to_contour.dml(
  model,
  cf.y,
  cf.d = cf.y,
  rho2 = 1,
  which.bound = c("lwr", "upr"),
  bound_label = "Scenario",
  level = 0.95,
  combine.method = "median",
  label.bump.x = 0.01,
  label.bump.y = 0.01,
  ...
)
}
\arguments{
\item{model}{an object of class \code{\link{dml}}.}

\item{cf.y}{partial R2 of omitted variables with the outcome.}

\item{cf.d}{strength of confounding in the Riesz representer.}

\item{rho2}{degree of adversity. Default is 1.}

\item{which.bound}{which bound was used for the contour plot: \code{"lwr"} or \code{"upr"}.}

\item{bound_label}{text label for the bound.}

\item{level}{confidence level. Default is 0.95.}

\item{combine.method}{method for combining cross-fitting estimates.}

\item{...}{additional arguments passed to \code{sensemakr::add_bound_to_contour}.}
}
\description{
This method allows you to easily add sensitivity bounds to an existing contour plot created with \code{\link{ovb_contour_plot}}.
It computes the bound value automatically from the \code{\link{dml}} model, so you only need to provide \code{cf.y} and \code{cf.d}.
}
