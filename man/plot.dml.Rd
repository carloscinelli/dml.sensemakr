% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.dml}
\alias{plot.dml}
\alias{plot.dml.bounds}
\title{Coefficient plots for DML and bounds}
\usage{
\method{plot}{dml}(x, combine.method = "median", level = 0.95, ...)

\method{plot}{dml.bounds}(
  x,
  type = c("confidence_bounds", "all"),
  combine.method = "median",
  level = 0.95,
  max = TRUE,
  ...
)
}
\arguments{
\item{x}{an object of class \code{\link{dml}} or \code{\link{dml_bounds}}.}

\item{combine.method}{method to combine the results of each repetition of the DML fit. Options are \code{mean} and \code{median}. Default is \code{median}.}

\item{level}{confidence level. Default is \code{0.95}.}

\item{...}{arguments passed to other methods.}

\item{type}{type of plot for confidence bounds. Options are \code{confidence_bounds}, and \code{all}.}

\item{max}{logical. Should confidence-bound plots use the widest confidence
envelope over all weaker confounding? Default is \code{TRUE}.}
}
\value{
For \code{plot.dml}: a \code{ggplot} object. For \code{plot.dml.bounds}: a \code{ggplot} object.
}
\description{
Coefficient plots for DML and bounds
}
