% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print-summary-dml.R
\name{summary.dml}
\alias{summary.dml}
\alias{coef.dml}
\alias{se}
\alias{se.dml}
\alias{confint.dml}
\alias{print.summary_dml}
\alias{print.dml}
\title{coef, se, confint, print and summary methods for DML}
\usage{
\method{summary}{dml}(object, combine.method = "median", ...)

\method{coef}{dml}(object, combine.method = "median", ...)

se(object, ...)

\method{se}{dml}(object, combine.method = "median", ...)

\method{confint}{dml}(object, params = NULL, level = 0.95, combine.method = "median", ...)

\method{print}{summary_dml}(x, interpret = F, ...)

\method{print}{dml}(
  x,
  digits = max(3L, getOption("digits") - 3L),
  combine.method = "median",
  ...
)
}
\arguments{
\item{object}{an object of class \code{\link{dml}}.}

\item{combine.method}{method to combine the results of each repetition of the DML fit. Options are \code{mean} and \code{median}. Default is \code{median}.}

\item{...}{arguments passed to other methods.}

\item{params}{character vector with the names of parameters.}

\item{level}{confidence level. Default is \code{0.95}.}

\item{x}{an object of class \code{\link{dml}}.}

\item{digits}{minimal number of significant digits.}
}
\description{
The \code{print} and \code{summary} methods provide descriptions of the results obtained with the function \code{\link{dml}}.

The \code{coef} function extracts the coefficients.

The \code{se} function extracts the standard errors.

The \code{confint} function extracts the standard errors.
}
\examples{
# loads package
library(dml.sensemakr)

## loads data
data("pension")

# set the outcome
y <- pension$net_tfa  # net total financial assets

# set the treatment
d <- pension$e401    # 401K eligibility

# set the covariates (a matrix)
x <- model.matrix(~ -1 + age + inc  + educ+ fsize + marr + twoearn + pira + hown, data = pension)

## compute income quartiles for group ATE.
g1 <- cut(x[,"inc"], quantile(x[,"inc"], c(0, 0.25,.5,.75,1), na.rm = TRUE),
          labels = c("q1", "q2", "q3", "q4"), include.lowest = T)

# run DML (nonparametric model)
## 2 folds (change as needed)
## 1 repetition (change as needed)
dml.401k <- dml(y, d, x, model = "npm", groups = g1, cf.folds = 2, cf.reps = 1)


summary(dml.401k)
summary(dml.401k, combine.method = "mean")
coef(dml.401k)
coef(dml.401k, combine.method = "mean")
se(dml.401k)
confint(dml.401k, combine.method = "mean")

}
